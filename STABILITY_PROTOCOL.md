# بروتوكول استقرار نظام MyAtelier Pro (Stability Protocol)

يجب على أي مساعد ذكاء اصطناعي (AI Assistant) يشارك في تطوير هذا المشروع الالتزام بالقواعد التالية لضمان عدم انهيار النظام:

## 1. سياسة "الاختبار أولاً" (Test-Driven Development)
* **ممنوع الدمج** في الفرع الرئيسي `main` إلا بعد تشغيل كافة الاختبارات الآلية بنجاح.
* الأمر المطلوب تشغيله: `python -m pytest backend/tests/`
* يجب أن تكون النتيجة دائماً: **147 passed, 0 failed**.

## 2. معايير جودة الكود (Code Guardrails)
* **حجم الملفات:** يمنع تجاوز أي ملف صفحة في الواجهة الأمامية (`frontend/src/pages/*.tsx`) لـ **250 سطراً**.
* في حال اقتراب الملف من هذا الحد، يجب استخراج المنطق (State, Queries, Mutations) إلى Hooks منفصلة في مجلد الـ `features` المقابل.

## 3. العمليات المالية والتدقيق
* **البيانات المالية:** استخدم دائماً `Decimal` للعمليات الحسابية ولا تخلطها مع `float`.
* **التدقيق (Audit):** أي مسار بريد (Endpoint) يقوم بعملية كتابة (POST, PATCH, DELETE) يجب تسجيله في `write_route_audit_policy.py` مع استدعاء `record_audit` داخل الكود.

## 4. الموثوقية (Reliability)
* عند تحديث أي Schema في قاعدة البيانات، يجب فوراً تحديث ملفات الـ `tests/conftest.py` أو ملفات الاختبار المقابلة لضمان عدم كسر الاختبارات بسبب حقول إجبارية جديدة.

---
**تم اعتماد هذا البروتوكول في 8 مايو 2026 لضمان استمرارية العمل باحترافية.**
