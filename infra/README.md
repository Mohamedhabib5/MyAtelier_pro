# Infra Notes

- `docker-compose.yml` is the default local orchestration entry point.
- PostgreSQL 16 is the primary database target for local/dev parity.
- Additional deployment manifests can be added here in later milestones.